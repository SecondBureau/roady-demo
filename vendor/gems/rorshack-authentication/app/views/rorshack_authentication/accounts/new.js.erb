$("#register_form").fadeIn().html("<%= escape_javascript(render('form', :account => @account, :authorizations => @authorizations, :title => raw(t '.title'), :update => false)) %>")
$("fieldset.inputs > legend > span > span").append(' et jouer');
$("#register_form").prepend('<div id="close_form" />');
// TODO: Specific Special Event.
// To remove or to refactor
var oldButton = jQuery("#account_submit");
var newButton = oldButton.clone();
newButton.attr("type", "image");
newButton.attr("id", "account_submit2");
newButton.insertBefore(oldButton);
oldButton.remove();
newButton.attr("id", "account_submit")
$("#account_submit").attr('src', '/images/decoration/easter/register-form-submit.png');
